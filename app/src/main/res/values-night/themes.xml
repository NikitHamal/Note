<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
    Base application theme for night mode.
    This file is needed for dark mode, but the content can be the same
    as the values/themes.xml because Theme.Material3.DayNight handles the switching.
    -->
    <style name="AppTheme.M3" parent="@style/Theme.Material3.DayNight.DynamicColors">
        <!-- Add any top-level dark-mode-specific customizations here if needed -->
    </style>
</resources>
